/* Optional overrides, loaded before the app.
   A static site cannot keep a tile key secret — the real control is the
   domain allow-list in your MapTiler dashboard. */
window.SAIL_CONFIG = {
  // maptilerKey: 'your-own-key-here',
};
